{
  "name": "javarosa-js-workspace",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" run build",
    "clean": "pnpm -r --filter \"./packages/*\" run clean",
    "test": "pnpm -r --filter \"./packages/*\" run test || echo \"tests pending\""
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.6",
    "tsup": "^8.1.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}